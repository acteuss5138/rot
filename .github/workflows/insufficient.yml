name: fraction

on:
  push:

jobs:
  lot:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        archive: ["inquiry", "definitely", "prospect", "call", "draught"]
        tremendous: ["cast", "fisherman", "trail", "else"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar hs.jar
